# Skill: LLM Council — Adversarial Decision Review

**Moved.** This is now a registered Claude Code skill so it auto-invokes by relevance
without being named. Canonical source:

`.claude/skills/llm-council/SKILL.md`

Run it by describing a consequential, hard-to-reverse decision — Claude triggers the
council itself. To make it available in every project (not just this repo), copy the
skill folder to `~/.claude/skills/llm-council/` on your machine.
